# Codeception Test Suite Configuration

# Suite for integration tests.
# Load WordPress and test classes that rely on its functions and classes.


class_name: IntegrationTester
modules:
    enabled:
        - \Helper\Integration
        - WPLoader
    config:
        WPLoader:
            wpRootFolder: /Users/anderson/Projects/OSTraining/Git/dev-env-wordpress/www
            dbName: database
            dbHost: localhost
            dbUser: root
            dbPassword:
            tablePrefix: int_
            domain: localhost:32777
            adminEmail: anderson@ostraining.com
            title: WP Tests
            plugins:
            activatePlugins:
            bootstrapActions: []
