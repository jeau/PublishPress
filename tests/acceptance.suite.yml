# Codeception Test Suite Configuration

# Suite for WordPress acceptance tests.
# Perform tests using or simulating a browser.


class_name: AcceptanceTester
modules:
    enabled:
        - \Helper\Acceptance
        - WPDb
        - WPBrowser
    config:
        WPDb:
            dsn: 'mysql:host=localhost;dbname=wp_tests'
            user: root
            password:
            dump: tests/_data/dump.sql
            populate: true
            cleanup: true
            url: 'http://localhost:32777'
            tablePrefix: wp_
        WPBrowser:
            url: 'http://localhost:32777'
            adminUsername: admin
            adminPassword: admin
            adminPath: /wp-admin
